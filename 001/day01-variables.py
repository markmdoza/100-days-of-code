# name = input("What is your name?")
# print(name)

# print(len(input()))
name = input('What is your name?')
length = len(name)
print(length)
# create a third variable to switch two variable inputs.
# variables with more than one word need to be separated by an underscore.
